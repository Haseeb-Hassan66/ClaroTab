// Copy this file to "config.js" (same folder) and paste in your real key.
// config.js is gitignored -- it will never be committed, so your key stays private.

export const GEMINI_API_KEY = "YOUR_GEMINI_API_KEY_HERE";