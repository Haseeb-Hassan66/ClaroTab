# Secrets -- the Gemini API key now lives in chrome.storage.local via the
# Options page, not a file. Kept here defensively in case anyone still has
# an old local config.js from before that change.
config.js
.env
*.key

# OS / editor junk
.DS_Store
Thumbs.db
.vscode/

# Dependencies (if we add any tooling later)
node_modules/